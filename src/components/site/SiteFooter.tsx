export default function SiteFooter() {
  return (
    <footer className="mx-auto w-full max-w-6xl px-6 pb-12 pt-10 text-sm text-ink/60">
      <div className="flex flex-col gap-2 border-t border-white/50 pt-6 md:flex-row md:items-center md:justify-between">
        <span>Photo Wall Studio · 用主题和故事把照片变成纪念</span>
        <span>本地保存 · 无需登录 · 支持高清导出</span>
      </div>
    </footer>
  );
}
